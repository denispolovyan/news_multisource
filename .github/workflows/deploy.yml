name: Deploy Vue App to GitHub Pages

on:
  push:
    branches:
      - main  # 👈 деплой при пуші у main (зміни за потреби)

permissions:
  contents: write  # 👈 дає права GitHub Actions на пуш у gh-pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Клонуємо репозиторій
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Встановлюємо Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18  # або 20, якщо твій проєкт підтримує

      # 3️⃣ Встановлюємо залежності
      - name: Install dependencies
        run: npm ci

      # 4️⃣ Збираємо проєкт
      - name: Build project
        run: npm run build

      # 5️⃣ Деплой на GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist   # 👈 для Vite або Vue CLI
          commit_message: "🚀 Deploy from GitHub Actions"
